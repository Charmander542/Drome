# Drome

A native iOS streaming client for a self-hosted Navidrome server, with a
Spotify-style interface, plus a small Go companion backend for the wishlist.

(Full documentation is written at the end of the initial build — see below.)
